spring.application.name=Jobify
server.port=8080
logging.level.org.springframework.security=DEBUG
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
#auth0.audience=https://${domain}/api/v2/
#spring.security.oauth2.resourceserver.jwt.issuer-uri=https://${domain}/
#spring.security.oauth2.resourceserver.jwt.audiences=https://${domain}/api/v2/
#spring.security.oauth2.resourceserver.jwt.jwk-set-uri=https://${domain}/.well-known/jwks.json
#jwt.expiration=86400000

spring.config.import=optional:secrets.properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password = ${DB_PASSWORD}
spring.jpa.properties.hibernate.default_schema=defaultdb

#Smtp email application-dev.properties

#spring.mail.host=${MAIL_HOST}
#spring.mail.port=${MAIL_PORT}
#mail.email = ${MAIL_EMAIL}
#spring.mail.username=${MAIL_USERNAME}
#spring.mail.password=${MAIL_PASSWORD}
#spring.mail.properties.mail.smtp.auth=true
#spring.mail.properties.mail.smtp.starttls.enable=true
#spring.mail.properties.mail.debug=true
#
secret.key.string = ${SecretKey}
